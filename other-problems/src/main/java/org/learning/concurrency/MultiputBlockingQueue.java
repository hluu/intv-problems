package org.learning.concurrency;

/**
 * https://www.careercup.com/question?id=5707470304444416
 * Write a thread-safe bounded blocking queue supporting multiple
 * producers and consumers and atomic-multiput method
 *
 * ( where a list of objects will be put into the buffer in consecutive
 *   order in the presence of concurrent puts).
 *
 * The list being passed as multi-put argument can be bigger than
 * the capacity of the buffer. Below is the interface to implement.
 */
public class MultiputBlockingQueue {
}
